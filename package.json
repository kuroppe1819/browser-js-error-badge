{
  "name": "browser-js-error-badge",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dist": "mkdir -p ./dist && cp -rf public/* src/* ./dist/",
    "prepare": "husky install"
  },
  "author": "kuroppe1819",
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "^17.5.1",
    "@commitlint/config-conventional": "^17.4.4",
    "eslint": "^8.38.0",
    "eslint-config-prettier": "^8.8.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.1",
    "prettier": "^2.8.7"
  },
  "eslintConfig": {
    "extends": [
      "eslint:recommended",
      "prettier"
    ],
    "env": {
      "browser": true,
      "es2022": true,
      "webextensions": true
    },
    "ignorePatterns": [
      "dist/*"
    ]
  },
  "lint-staged": {
    "*.{js,css,md}": [
      "prettier --write",
      "eslint ./"
    ]
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
